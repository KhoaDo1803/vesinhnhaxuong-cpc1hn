import {sql} from "drizzle-orm";
import {index,integer,sqliteTable,text} from "drizzle-orm/sqlite-core";
export const cleaningRecords=sqliteTable("cleaning_records",{id:integer("id").primaryKey({autoIncrement:true}),area:text("area").notNull(),line:text("line").notNull(),chemical:text("chemical").notNull(),operator:text("operator").notNull(),result:text("result").notNull().default("Đạt"),ipcStatus:text("ipc_status").notNull().default("Chờ đối chiếu"),cleanedAt:text("cleaned_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_cleaning_records_ipc_status").on(t.ipcStatus),index("idx_cleaning_records_cleaned_at").on(t.cleanedAt)]);
export const chemicalBatches=sqliteTable("chemical_batches",{id:integer("id").primaryKey({autoIncrement:true}),chemicalName:text("chemical_name").notNull(),batchCode:text("batch_code").notNull().unique(),preparedAt:text("prepared_at").notNull(),expiresAt:text("expires_at").notNull(),status:text("status").notNull().default("Được phép sử dụng")},t=>[index("idx_chemical_batches_expires_at").on(t.expiresAt)]);
